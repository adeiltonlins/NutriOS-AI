# React UI source

A interface React adaptada é derivada do arquivo do AI Studio enviado para análise. Ela está sendo integrada somente nesta branch de trabalho.

Antes do merge final, o fonte React será materializado em `frontend/`, passará por `npm run lint` e `npm run build`, e o build será publicado em `app/static/react-ui/`.
